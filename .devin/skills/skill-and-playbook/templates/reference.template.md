# {{topic}}（与该技能强相关 · 随技能加载）

> 装**与该技能强相关的一切**：事实型规格（API 端点·参数·字段·返回、错误码、凭证、数字阈值、机读 schema、退出码）**＋** 该技能的排错经验/踩坑/根因/认知。判据：**离开这个技能就没意义？** 是 → 放这。
> 随 `.devin/skills/` 同步走、自包含——技能能独立跑，不依赖中心 `playbooks/`。配 `skills/{{name}}/SKILL.md` 一起看。
> 内容多可拆多份：事实放 `references/{{provider}}-api.md`，排错/认知放 `references/{{name}}-notes.md`，agent 只读相关那份。
> 与具体技能**不强相关**的通用经验（跨工具运维、日常通用错误、秘诀妙招）才归中心 `playbooks/<name>.md`，不在这。

## 目录（>300 行时保留，短文档可删）

- 1. {{Base / Auth / 红线}}
- 2. {{端点 / 参数 / 字段}}
- 3. {{错误码 / 阈值 / schema}}
- 4. {{根因 / 踩坑 / 排错心法}}

## 1. {{Base / Auth / 红线}}

{{基础地址、鉴权 header、绝不能做的硬约束……纯事实}}

## 2. {{端点 / 参数 / 字段}}

| {{方法/参数}} | {{端点/默认}} | {{说明}} |
|---|---|---|
| {{...}} | {{...}} | {{...}} |

## 3. {{错误码 / 阈值 / 机读 schema}}

| {{码/字段}} | {{可重试?/类型}} | {{含义}} |
|---|---|---|
| {{...}} | {{...}} | {{...}} |

## 4. {{根因 / 踩坑 / 排错心法（该技能强相关的认知，也属本层）}}

{{为什么这么设计、被坑后的认知升级、现象→判据→处置；与该技能强绑定就留这，别外推到 playbook}}
