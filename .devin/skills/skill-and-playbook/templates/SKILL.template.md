---
name: {{kebab-case-name}}
description: {{一句"做什么"}}。只要用户提到：{{触发词1}} / {{触发词2}} / {{触发词3}}…… 就用本技能。它会{{核心动作概述}}，默认{{关键默认/约束}}。
---

# {{name}}（{{一句话中文定位}}）

{{一句话：这个技能让 agent 能做什么、解决什么痛点}}。质量标杆对照 `image-gen`。

## Quick Start（30 秒能跑）

{{最常用的一条命令 / 最小可用示例；能跑就直接贴命令，别只描述}}

```{{lang}}
{{command --foo ... --bar ...}}
```

- 默认：{{尺寸 / 格式 / 数量 / 路径等默认值，省得每次问用户}}
- 完整参数·字段·错误码（查表照抄）→ `references/{{topic}}-api.md`
- 根因·设计哲学·踩坑心法（出事才翻）→ `playbooks/{{name}}.md`

## 触发与默认

- **触发**：{{什么场景该自动调用；用户会怎么说}}
- **默认值**：{{逐项列出默认，能不问用户就别问}}

## 命令 / 用法

{{把 happy-path 的几条命令列清楚，用祈使句；只留"顺利做好任务"必需的}}

## 参数（只列常用；完整白名单见 references/）

| 参数 | 默认 | 说明 |
|---|---|---|
| {{--foo}} | {{默认}} | {{一句话}} |
| {{--bar}} | {{默认}} | {{一句话}} |

## 指针（厚内容都在下面两处，本文件不展开）

- 事实型规格：`references/{{topic}}-api.md`
- 沉淀认知：`playbooks/{{name}}.md`

<!--
写作自查（删除本注释块再交付）：
- description 写清"做什么 + 什么时候用"，且足够 pushy（宁可多触发，别欠触发）；无尖括号；≤1024 字符。
- 正文 <500 行；只留 happy-path；厚事实下沉 references/、厚认知下沉 playbooks/，本文件只放指针。
- 新建/改动后去 AGENTS.md 知识索引加/改一行（见 agents-index-line.snippet.md）。
-->
